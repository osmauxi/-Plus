# 地图结构缩减修改链路

最后更新：2026-08-09

## 1. 修改目标

本轮只缩减地图生成、视觉构建和网络同步之间的重复数据结构，不修改以下行为：

- 地图随机生成算法与 Seed 规则。
- 房间模板选择、权重和旋转规则。
- 房间、连接的实际实例化流程。
- LocalObjectPool 的 Prepare、Rent、Return、Release 生命周期。
- Client Ready、超时、取消旧构建和清理地图流程。

## 2. 修改前的数据链路

```text
Server
MapGenerationRequest
→ MapLayout
→ MapVisualPlan
→ MapLayoutSnapshot
→ ClientRpc

Client
MapLayoutSnapshot
→ MapLayout
→ MapVisualPlan
→ MapVisualBuilder
```

其中每个房间会依次存在三套高度重复的数据：

```text
MapRoomDefinition
RoomVisualDefinition
MapRoomSnapshot
```

连接也会在以下两个结构之间重复转换：

```text
MapConnectionDefinition
MapConnectionSnapshot
```

## 3. 修改后的数据链路

```text
Server
MapGenerationRequest
→ MapLayout
→ MapBuildPlan
├─→ MapVisualBuilder
└─→ ClientRpc

Client
MapBuildPlan
├─→ MapVisualBuilder
└─→ MapLayout（仅在需要逻辑查询时通过 ToLayout 恢复）
```

Server 本地构建和 ClientRpc 现在使用同一份 `MapBuildPlan`。Client 接收后可以直接构建，不再执行 Snapshot 与 VisualPlan 之间的双向转换。

## 4. 类型调整

### 4.1 保留的生成阶段类型

- `MapGenerationSettings`：地图生成配置。
- `MapGenerationRequest`：单次地图生成请求。
- `MapRoomDefinition`：尚未选择视觉模板的逻辑房间。
- `MapConnectionDefinition`：房间逻辑连接，同时复用于构建和网络同步。
- `MapLayout`：Server 生成得到的完整逻辑布局。

这些类型具有独立职责，没有为了减少数量而强行合并。

### 4.2 新增的最终构建类型

`RoomVisualDefinition` 被替换为 `MapRoomBuildDefinition`。

它保存以下不可推导数据：

```text
RoomId
RoomType
TemplateId
WorldPosition
LayoutRotation
DistanceFromStart
RequiredWorldConnectors
RotationIndex
```

以下数据改成计算属性，不再重复保存和同步：

```text
WorldRotation
RequiredLocalConnectors
```

计算关系为：

```text
RotationIndex
→ Quaternion.Euler(0, RotationIndex × 90, 0)
→ WorldRotation

RequiredWorldConnectors + RotationIndex
→ ConnectorMaskUtility.WorldToLocal
→ RequiredLocalConnectors
```

因此每个网络房间至少减少一个 Quaternion 和一个 byte 的重复数据。

### 4.3 MapBuildPlan

原 `MapVisualPlan` 被替换为 `MapBuildPlan`，并直接实现 `INetworkSerializable`。

`MapBuildPlan` 持有：

```text
Seed
GenerationAttempt
StrategyType
StrategyVersion
StartRoomId
BossRoomId
MapRoomBuildDefinition[]
MapConnectionDefinition[]
```

它负责：

- 作为 Server 和 Client 共用的最终地图构建数据。
- 手动序列化不可变的房间和连接结构。
- 校验房间数量、连接数量、重复 ID、非法枚举、非法旋转、起点、Boss 和连接端点。
- 通过 `ToLayout()` 在 Client 恢复逻辑查询所需的 `MapLayout`。

为了避免网络结构携带易失效的运行时缓存，旧 `MapVisualPlan._roomsById` 字典已删除。

### 4.4 删除的类型

以下类型及其相互转换已删除：

```text
MapRoomSnapshot
MapConnectionSnapshot
MapLayoutSnapshot
MapVisualPlan
RoomVisualDefinition
```

对应的 `MapLayoutSnapshot.cs` 和 `.meta` 已删除。

原 `MapVisualPlan.cs` 已改名为 `MapBuildPlan.cs`，并保留原 `.meta` GUID，避免 Unity 资源身份发生不必要变化。

## 5. 各脚本修改说明

### `MapBuildPlan.cs`

- 新增 `MapRoomBuildDefinition`。
- 新增网络可序列化的 `MapBuildPlan`。
- 集中实现数据校验、网络序列化与 `ToLayout()`。
- 删除房间索引字典和 Snapshot 转换方法。

### `GridRoomTemplateSelector.cs`

- 返回值由 `MapVisualPlan` 改为 `MapBuildPlan`。
- 模板选择完成后直接创建 `MapRoomBuildDefinition`。
- 不再提前保存 `WorldRotation` 和 `RequiredLocalConnectors`，两者由最终数据按需计算。

### `MapGenerationController.cs`

- `CurrentVisualPlan` 与 `CurrentSnapshot` 合并为 `CurrentBuildPlan`。
- Server 本地构建与网络发送复用同一个 `MapBuildPlan`。
- ClientRpc 改为传递 `generationId + MapBuildPlan`。
- Client 不再调用 `snapshot.ToVisualPlan()`，接收后直接验证和构建。
- Client 构建成功后通过 `buildPlan.ToLayout()` 恢复 `CurrentLayout`，保持原有逻辑布局访问能力。
- 重连发送方法由 `SendCurrentSnapshotToClientServer` 改为 `SendCurrentMapToClientServer`。
- Clear 和 Shutdown 统一将 `CurrentBuildPlan` 设为 `default`。

### `MapVisualBuilder.cs`

- 构建入口改为 `BuildAsync(MapBuildPlan, CancellationToken)`。
- 房间和 Pool 收集改用 `MapRoomBuildDefinition`。
- 连接直接遍历 `buildPlan.Connections`，不再依赖 `visualPlan.Layout`。
- 连接方向使用已经实例化的 `RoomViewRuntime.View.transform.position`，不再额外查询视觉房间字典。
- 上一轮完成的 Pool Prepare、Old-New Release、Clear 和 Shutdown 行为保持不变。

### `MapBuildResult.cs`

- `VisualPlan` 属性替换为 `BuildPlan`。
- 构造时验证传入的 `MapBuildPlan`。

### `HotFix.Gameplay.csproj`

- 编译项由 `MapVisualPlan.cs` 更新为 `MapBuildPlan.cs`。
- 删除已经不存在的 `MapLayoutSnapshot.cs` 编译项。

## 6. 网络与兼容性变化

- 地图 RPC 的参数类型已经改变，Server 与 Client 必须使用同一版本代码。
- `GenerationId` 仍由 `MapGenerationController` 单独管理，不混入地图静态内容。
- 地图算法版本 `StrategyVersion` 没有递增，因为本轮没有修改生成算法和随机结果。
- `MapBuildPlan` 仍同步原 Snapshot 中用于恢复逻辑地图的 Seed、策略、起点、Boss、距离和布局旋转数据。
- 当前仓库中没有发现旧 `CurrentVisualPlan`、`CurrentSnapshot`、`VisualPlan` 属性或重连方法的外部调用方。

## 7. 文件编码

本轮修改涉及的旧 GBK 脚本已转换为 UTF-8：

- `MapBuildPlan.cs`（原 `MapVisualPlan.cs`）
- `GridRoomTemplateSelector.cs`
- `MapBuildResult.cs`
- `MapGenerationController.cs`

`MapVisualBuilder.cs` 已在上一轮转换为 UTF-8。

## 8. 验证结果

执行：

```text
dotnet build HotFix.Gameplay.csproj --no-restore --nologo
```

结果：

```text
0 errors
7 warnings
```

7 个警告均来自未在本轮处理的既有 `OnDestroy` 隐藏、未赋值字段或未使用字段，没有新增编译错误。

后续进入 Unity 联机运行验证时，建议覆盖：

1. Host 生成地图并完成本地构建。
2. Remote Client 接收同一份 `MapBuildPlan` 并完成构建。
3. Host 与 Client 的房间 ID、位置、模板、旋转和连接完全一致。
4. 重连客户端能收到 `CurrentBuildPlan`。
5. 连续切层、取消旧构建、显式 Clear 和 Shutdown 后 Pool 租借计数归零。

## 9. RoomTemplate 配表接入 ConfigManager

### 9.1 配表与生成产物

`Assets/DesignData/Excels/RoomConfig.xlsx` 已从空白工作簿改为正式配置表：

- `RoomTemplate`：供数据管线生成 `Config_RoomTemplate`。
- `#说明`：记录 `RoomType`、连接掩码、旋转掩码和对象池字段的填写约定；工作表名以 `#` 开头，因此不会进入代码生成。

`RoomTemplate` 的字段为：

```text
TemplateId
RoomType
PoolId
SupportedConnectorMask
AllowUnusedConnectors
AllowedRotations
Priority
Weight
```

首批 6 条模板沿用旧房间池标识：

```text
Room_-1_0  Start
Room_-2_0  Boss
Room_1_0   Combat
Room_1_1   Combat
Room_1_2   Combat
Room_2_0   Treasure
```

Unity 数据管线已重新生成：

- `Config_RoomTemplate.cs`
- `ConfigRegister.cs` 中的 `Config_RoomTemplate` 注册分支
- `Config_RoomTemplate.bytes`
- `MessagePackGenerated.cs` 中的 AOT Formatter

`Config_RoomTemplate.bytes` 已加入 Addressables `Configs` Group，地址为 `Config_RoomTemplate`，标签为 `Config`，因此会被 `ConfigManager.LoadAllConfigsAsync()` 自动加载。

### 9.2 RoomTemplateCatalog

`RoomTemplateCatalog` 已移除 Inspector 手工维护的 `_templates` 序列化列表，初始化改为：

```text
ConfigManager.GetTable<Config_RoomTemplate>()
→ TemplateId 排序
→ 配表主键与字段合法性校验
→ RoomTemplateConfig 只读运行时对象
→ List + Dictionary 索引
```

按 `TemplateId` 排序用于固定候选模板顺序，避免字典枚举顺序改变同一 Seed 下的权重随机结果。

`RoomTemplateConfig` 不再承担 Unity 序列化职责，只保存从配表转换后的只读数据，并校验：

- `RoomType` 是否为有效枚举。
- `PoolId` 是否为空。
- 连接与旋转掩码是否越界。
- 权重是否为大于 0 的有限数值。

Shutdown 时会同时清理顺序列表和 TemplateId 索引，避免下次初始化残留旧配表数据。

### 9.3 对象池链路

房间实例化方式没有改变：

```text
RoomTemplateConfig.PoolId
→ MapVisualBuilder.CollectRequiredPoolIds
→ LocalObjectPool.PreparePoolsAsync
→ LocalObjectPool.Rent
→ LocalObjectPool.Return / ReleasePools
```

配表只替换了模板元数据来源，没有改成直接 `Instantiate`。

### 9.4 Unity MCP 场景挂载检查

检查场景：`Assets/_HotUpdate/Scenes/GameRunTimeScene.unity`。

正常项：

- `GameRoot/RoomTemplateCatalog` 已挂载 `RoomTemplateCatalog`，移除手工列表后只保留脚本引用，符合预期。
- `GameRoot/MapVisualBuilder` 已挂载 `MapVisualBuilder`，其 `_templateCatalog` 正确引用 `GameRoot/RoomTemplateCatalog`。
- `GameRoot/LocalObjectPool` 已挂载 `LocalObjectPool`，`_inactiveRoot` 有效。
- `GameRoot/MapGenerator` 已挂载 `MapGenerationController`，其 `_templateCatalog` 与 `_visualBuilder` 引用正确。
- `GameRuntimeBootstrap` 的完整服务顺序为 `GameNetworkRuntime → LocalObjectPool → SyncObjectPool → LocalVFXPool → PlayerManager → RoomTemplateCatalog → MapVisualBuilder → MapGenerationController`，依赖初始化顺序正确。
- `MapVisualBuilder._roomRoot`、`_connectionRoot` 和 `_templateCatalog` 均已绑定。

异常项：

- 当前代码中也没有其他 `GenerateAndBuildAsync` 调用方，因此进入 `MapGenerating` 状态后没有新地图编排器执行生成。
- `Config_LocalObjectPool.bytes` 当前反序列化结果为 0 条配置；RoomTemplate 中的 6 个房间 PoolId 没有被新 `LocalObjectPool` 登记。Grid Seamless 已不再要求 `Connection_Grid`。
- Unity MCP 扫描 `Assets/_HotUpdate/Prefabs` 的 69 个 Prefab 后，未发现任何根节点挂载 `RoomView`、包含 `RoomConnectorSlot` 或根节点挂载 `ConnectionView` 的 Prefab。

结论：Map 场景组件引用与启动顺序现在正常，但房间 Pool 配置和房间 Prefab 资源链路仍不完整，尚不能实际实例化地图。该检查仅作判断，本轮没有主动修改场景挂载或房间 Prefab。

### 9.5 验证

- Unity 编译诊断：`0 errors`。
- `Config_RoomTemplate.bytes` 回读：6 条记录，字段与 Excel 一致。
- `Config_RoomTemplate` Addressables 地址与 `Config` 标签已确认。
- `MessagePackGenerated.cs` 已包含 `Config_RoomTemplateFormatter`。

## 10. Grid Seamless、策略过滤与整层统一缩放

### 10.1 修改后的链路

```text
GridMapGenerationProfile.CreateSettings(seed)
├─ 从 AllowedRoomScales 确定本层唯一 RoomScale
└─ RoomSpacing = BaseRoomSize × RoomScale

GridGraphLayoutStrategy
├─ ConnectionMode = Seamless
├─ 使用 RoomSpacing 计算全部房间中心坐标
└─ MapLayout(RoomScale, Rooms, LogicalConnections)

RoomTemplateCatalog
└─ Config_RoomTemplate.AllowedStrategyMask

GridRoomTemplateSelector
├─ 先过滤 SupportsStrategy(GridGraph)
├─ 再执行 RoomType / Connector / Rotation / Priority / Weight 选择
└─ MapBuildPlan(ConnectionMode, RoomScale, Rooms, LogicalConnections)

Server
├─ 使用 MapBuildPlan 构建本机地图
└─ ClientRpc 同步同一份 MapBuildPlan

Client
└─ 直接读取 ConnectionMode 与 RoomScale 构建，不重新随机

MapVisualBuilder (Seamless)
├─ 从对象池 Rent 房间并设置整层统一 localScale
├─ 切换 RoomConnectorSlot 的封墙/开口
├─ 校验相邻 Connector 坐标重合且 forward 相反
├─ 登记逻辑 Connection 与双向 Portal
└─ 不准备连接 Pool，不 Rent ConnectionView
```

### 10.2 生成策略和同步数据

- `MapGenerationTypes.cs` 新增 `MapStrategyMask`、`ConnectionPresentationMode`，并把生成参数拆分为 `BaseRoomSize` 与 `RoomScale`。
- `IMapLayoutStrategy.cs` 新增 `ConnectionMode`，让每种策略显式声明是否需要 `ConnectionView`。
- `GridGraphLayoutStrategy.cs` 声明 `ConnectionMode = Seamless`，策略版本从 1 升为 2，并把 `RoomScale` 写入 `MapLayout`。
- `GridMapGenerationProfile.cs` 将旧 `_roomSpacing` 迁移为 `_baseRoomSize`，新增默认 `[0.75, 1, 1.25]` 的 `_allowedRoomScales`。Scale 使用独立 Seed 随机流，不改变拓扑随机序列。
- `MapBuildPlan.cs` 新增并网络同步 `ConnectionMode` 与 `RoomScale`。Client 不依赖本地策略推断，也不会重新选择 Scale。
- `MapGenerationController.cs` 改为先确定 Seed，再创建本层生成设置，并将策略的 `ConnectionMode` 写入最终构建计划。

### 10.3 房间策略过滤

`RoomConfig.xlsx / RoomTemplate` 新增：

```text
AllowedStrategyMask:int
1 = GridGraph
2 = Corridor
3 = GridGraph + Corridor
```

现有 6 个通用模板均配置为 `3`。未来异形房间只要不包含 GridGraph 位，就不会进入 Grid 候选列表。`RoomTemplateConfig` 增加掩码合法性校验和 `SupportsStrategy()`，`GridRoomTemplateSelector` 在房间类型、方向和旋转过滤之前先进行策略过滤。

### 10.4 Grid 无 ConnectionView 构建

- `MapVisualBuilder` 在 Seamless 模式下不会检查、Prepare 或 Rent `_gridConnectionPoolId`。
- 逻辑连接仍然保留，并解析两端 `RoomConnectorSlot`，用于开墙、Portal、房间邻接和战斗门逻辑。
- 新增 `_seamPositionTolerance=0.02`。相邻插槽的世界坐标必须在 `Tolerance × RoomScale` 内重合，forward 必须相反，否则构建立即失败。
- `RoomConnectorSlot` 新增可选 `_battleGateRoot`。Grid 战斗封门落在两端房间插槽；需要走廊的策略仍使用 `ConnectionView.BattleGateRoot`。
- `ConnectionViewRuntime` 统一包装 Seamless 和 ConnectionView 两种连接；`RoomPortalRuntime` 新增明确的 `InwardNormal`。

### 10.5 Grid 资源约束

- 房间为相同基础边长的正方形 Plane。
- Prefab 根 Pivot 位于底面严格中心。
- `_baseRoomSize` 等于 Scale=1 时的实际边长。
- 四方向 Connector 位于边界开口中心，forward 朝房外。
- Grid 仍要求房间正确挂载 `RoomConnectorSlot`，因为同一模板可能被其他策略复用，且 Grid 本身仍用插槽切墙、登记 Portal 和控制战斗门。

运行时关系为：

```text
RoomSpacing = BaseRoomSize × RoomScale
RoomRoot.localScale = Vector3.one × RoomScale
WorldPosition = GridCoordinate × RoomSpacing
```

### 10.6 自动生成与验证

已重新生成：

- `Config_RoomTemplate.cs`
- `ConfigRegister.cs`
- `Config_RoomTemplate.bytes`
- `MessagePackGenerated.cs`

验证结果：

- Unity 完整脚本编译：`0 errors / 0 warnings`。
- 配表渲染检查与错误值扫描正常。
- 二进制为 9 字段、6 行，Unity 内成功反序列化，所有 `AllowedStrategyMask` 均为 `3`。
- `MapBuildPlan` 经 Netcode `FastBufferWriter/FastBufferReader` 往返后仍为 `Mode=Seamless`、`Scale=1.25`、15 个房间和 14 条连接。
- 规则测试 Seed `20260809`：`Scale=0.75`、`Spacing=52.5`、13 个房间、12 条逻辑连接、`Mode=Seamless`。
- 测试中的 Corridor-only 高优先级模板没有被 Grid 选中。

## 11. Grid 起始房、怪物房与 Boss 房资源注册

### 11.1 Prefab 资产

以场景根对象 `Room` 为唯一来源，原样保存了三个独立的 Regular Prefab：

```text
Assets/_HotUpdate/Prefabs/Rooms/Room_Grid_Start.prefab
Assets/_HotUpdate/Prefabs/Rooms/Room_Grid_Monster.prefab
Assets/_HotUpdate/Prefabs/Rooms/Room_Grid_Boss.prefab
```

三个 Prefab 只区分资源名与根节点名，未修改内部层级、组件和引用。三者均加入 Addressables `Prefabs` Group，地址分别与资源名一致。

### 11.2 RoomTemplate 与 LocalObjectPool

`RoomConfig.xlsx / RoomTemplate` 已移除旧占位数据，改为三条仅允许 GridGraph 使用的模板：

| TemplateId | RoomType | AllowedStrategyMask | PoolId |
|---:|---:|---:|---|
| 1 | 1（Start） | 1（GridGraph） | `Room_Grid_Start` |
| 2 | 6（Boss） | 1（GridGraph） | `Room_Grid_Boss` |
| 3 | 2（Combat） | 1（GridGraph） | `Room_Grid_Monster` |

`PoolConfig.xlsx / LocalObjectPool` 已登记对应本地对象池：

| ConfigId | PoolId / PrefabAddress | GroupName | InitialCapacity | MaxSize |
|---:|---|---|---:|---:|
| 1 | `Room_Grid_Start` | `MapRoom` | 1 | 1 |
| 2 | `Room_Grid_Monster` | `MapRoom` | 4 | 16 |
| 3 | `Room_Grid_Boss` | `MapRoom` | 1 | 1 |

完整运行链路为：

```text
Config_RoomTemplate.PoolId
→ RoomTemplateCatalog
→ MapVisualBuilder.CollectRequiredPoolIds
→ Config_LocalObjectPool.PoolId
→ LocalObjectPool.PreparePoolAsync
→ Addressables.LoadAssetAsync(PrefabAddress)
→ LocalObjectPool.Rent
```

本次只修改了 Excel 数据行，没有改变表结构，因此没有重生成配置类或 MPC Formatter；已重新导出全部配置二进制。

### 11.3 验证结果

- 两张 Excel 表均完成数据回读、错误值扫描和渲染复核。
- `Config_RoomTemplate.bytes` 回读为 3 条，`Config_LocalObjectPool.bytes` 回读为 3 条。
- 三个 Addressable 地址均能在 Editor 内成功加载到对应 Prefab。
- 每个 Prefab 都包含 274 个 GameObject、708 个组件、1 个 `RoomView` 和 4 个 `RoomConnectorSlot`。
- 三个 Prefab 均无 Missing Script 或丢失对象引用；忽略根名称后的序列化 SHA-256 签名一致。
- Unity Console 为 0 error，源场景未被修改或标脏。

### 11.4 保留的 Room 资源问题

本次按需求原样复制并注册 `Room`，没有修正其 Connector。当前 West、South 插槽的 `forward` 仍与 East、North 同向，且四个锚点到房间中心的距离不一致；在 `_baseRoomSize=70`、`_seamPositionTolerance=0.02` 的 Grid Seamless 校验下仍可能构建失败。该问题属于后续房间基础 Prefab 校正，不属于本次注册修改。
